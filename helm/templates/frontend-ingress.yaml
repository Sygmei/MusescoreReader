apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "fumen.frontend.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "fumen.name" . }}
    app.kubernetes.io/component: frontend
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Release.Namespace }}-https-redirect-mw@kubernetescrd"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "{{ .Values.frontend.ingress.domain }}"
      secretName: {{ include "fumen.certificateSecretName" . }}
  rules:
    - host: "{{ .Values.frontend.ingress.domain }}"
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "fumen.backend.fullname" . }}
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "fumen.frontend.fullname" . }}
                port:
                  name: http
    - host: "www.{{ .Values.frontend.ingress.domain }}"
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "fumen.backend.fullname" . }}
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "fumen.frontend.fullname" . }}
                port:
                  name: http
